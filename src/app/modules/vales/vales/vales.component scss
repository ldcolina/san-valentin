.love-card {
  max-width: 350px;
  background: #fff5f8;
  border-radius: 15px;
  box-shadow: 0px 5px 10px rgba(0, 0, 0, 0.2);
  display: flex; /* Usamos Flexbox */
  flex-direction: column; /* Para que los elementos se apilen en columna */
  justify-content: space-between; /* Asegura que el contenido tenga espacio */
  padding: 20px;
  transition: transform 0.3s;
}

.love-card:hover {
  transform: scale(1.05);
}

.mat-card-actions {
  display: flex;
  justify-content: center; /* Esto centra el botón horizontalmente */
  margin-top: 20px;
}
